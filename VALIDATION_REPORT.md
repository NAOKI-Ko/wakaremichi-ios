# KareMichi 検証レポート

最終更新: 2026-07-30（Asia/Tokyo）  
環境: Xcode 26.6 / iOS 26.5 Simulator（iPhone 17e）/ iPhone 16（iOS 26.5.1）

## 結論

- iOS 17.0以上の設定を維持し、シミュレータ・実機向けの両方でビルド成功。
- 追加した境界条件を含むロジック／SpriteKit／SwiftData／音源テスト29件が全件成功。
- iPhone 17eシミュレータのUIテスト2件が全件成功。5旅人、広告再開2周、回数上限、5枚カード、同日結果復元まで通過。
- 接続中のiPhone 16へインストールして通常起動に成功。
- 実機の自動操作は端末側で認証がキャンセルされたため、実機でのスワイプ、聴感、ハプティクスは未判定。

## レイアウト追補（KareMichi (2).zip）

- [x] ZIP全体は上書きせず、`GameView.swift`のレイアウト差分だけを統合。
- [x] `SpriteView`からSwiftUI側の`.aspectRatio(1, contentMode: .fit)`を削除。
- [x] 「ここまでにする」を迷路オーバーレイから、タイトルと同じ常設上部バーへ移動。
- [x] HUDを高さ148pt、旅人92pt、ミニマップ120ptへ拡大し、余白を縮小。
- [x] 前回のbottom safe area修正は維持し、ZIP側の`.ignoresSafeArea(edges: .bottom)`は取り込んでいない。
- [x] `StaminaGauge`の比率幅は`Swift.max` / `Swift.min`を使用し、プロパティ名との衝突なし。
- [x] iPhone 17eでUIテスト2件が再度成功。上部バー、拡大HUD、早抜け、結果復元を確認。
- [x] 更新後のスクリーンショットで、HUD内容が下端の安全領域へはみ出していないことを確認。

ZIP側に戻っていた以下の変更は意図的に取り込んでいない: SwiftData診断ログの削除、UI／カードへのハプティクス追加、スキップ音の欠落、保存直後の結果分岐退行、体力0後も滑走を続ける処理、発見音をハプティクス設定で止める処理、アクセシビリティ識別子の削除。

## HUD装飾追補（KareMichi (3).zip）

- [x] `DividerOrnament`、`MinimapFrame`、`StaminaFrame`の3 imagesetだけを追加。
- [x] 上部バー下とHUD上へ区切り装飾を配置。
- [x] 体力バーを装飾枠の中央窓へ収め、残量比率・20%未満の赤表示を維持。
- [x] ミニマップCanvasを羊皮紙枠の中央窓へ収め、探索済みマスだけを描く処理を維持。
- [x] プレイヤー表示をタイルの0.78倍から0.92倍へ拡大。
- [x] 新規3画像のロードと、プレイヤー36.8×36.8pt（40ptタイル×0.92）を自動テストで確認。
- [x] iPhone 17eのUIテスト2件が成功し、装飾の枠合わせ、早抜け、結果復元、安全領域を確認。
- [x] 素材の大きな透明余白を考慮し、Divider／StaminaFrameは中央の実描画領域が適切に見えるようクリップして表示。

この追補でも、ZIP側に含まれていたSwiftData、体力停止、音／ハプティクス、結果分岐、発見音、アクセシビリティの退行コードは取り込んでいない。

## 広告再開追補（KareMichi (4).zip）

- [x] `AdProvider.swift` を追加し、1秒後に成功する `MockAdProvider` を接続。
- [x] ゴール・体力切れ・早抜けの確定前に、一時停止画面を挟むよう変更。
- [x] 広告成功後は位置・霧・軌跡を維持し、体力だけ1000へ回復して探索を再開。
- [x] ゴール演出中の再開でも `glowNode` のアクションを解除し、alpha／scale／positionを通常状態へ復元。
- [x] ゴールから離れた後も `everReachedGoal == true` と `RunLog.reachedGoal == true` を維持。
- [x] `maxReplaysPerDay == 2` を使い切ると回復APIが拒否し、UIから「続きを歩く」が消える。
- [x] `replayCount` をSwiftDataへ保存・復元し、診断コメントへ反映。
- [x] 広告完了はメインスレッドへ戻し、広告読込中は結果確定ボタンを無効にして状態競合を防止。
- [x] iPhone 17e UIテストで、早抜け→ダミー広告→再開を2周し、3回目の一時停止で広告選択肢が無いことを確認。

今回のZIPに戻っていた、体力のマス単位停止判定、ゴール優先、ワープ出口解決、
SwiftData診断ログ、現在結果優先、音／ハプティクス分離、発見音分離、
アクセシビリティ、安全領域の各退行コードは取り込んでいない。

## 物語統合追補（KareMichi.zip）

- [x] `Diagnosis.swift`へ鏡の文章生成と、通算3／7／14／21／30日目の物語断片を追加。
- [x] `ResultView.swift`へ`totalDayCount`と、条件付き6枚目の鏡カードを追加。
- [x] `GameView.swift`へ初回だけのプロローグと、一時停止画面の物語文言を追加。
- [x] 現在結果を保存済み結果へ置き換えない分岐、広告コールバックのメインスレッド化、
  UIハプティクス分離、アクセシビリティ識別子は従来の修正版を維持。
- [x] 通算6日では鏡なし、7日で鏡ありとなる境界をテスト。
- [x] 今日を除く`recentAxes`へ今日の軸を加え、通算7日目に正しく鏡を解放。
- [x] SwiftDataのインメモリストアへ7日分を保存・再取得し、最も低い
  「柔軟性」が鏡へ表示されることを確認。
- [x] iPhone 17e UIテストでプロローグが初回だけ表示され、次回は旅人選択へ進むことを確認。
- [x] 7日分のUIテストで7日目の物語断片と6枚目の鏡カードを確認。
- [x] 既存の広告再開2周、上限、結果保存、同日復元UIフローも再度成功。

ZIP側に含まれていた、保存直後の画面分岐退行、`GameHaptics`のUI／カードへの再追加、
アクセシビリティ削除、広告再開の競合防止削除、HUD装飾レイアウトの退行は取り込んでいない。

## はじめからやり直す追補（KareMichi (1).zip）

- [x] `MazeGame.resetForRestart()`を追加し、位置・歩数・霧・軌跡・宝箱・
  ゴール到達・診断ログをスタート状態へ戻す。
- [x] `replayCount + restartCount`で`maxReplaysPerDay`を共有し、上限後は
  「続きから」「はじめから」の両APIが状態を変更せず拒否する。
- [x] `MazeScene.restartAfterAd()`を防御付きBool APIとして追加。
- [x] 開いた宝箱を`ChestClosed`へ戻し、開封アクションとscaleも初期化。
- [x] 過去に探索したタイルをalpha 0へ戻し、スタート周辺だけ霧を晴らし直す。
- [x] 旅人、手元の光、カメラをスタート地点へ戻し、HUDを更新。
- [x] `restartCount`をSwiftDataへ保存・復元し、既存の診断根拠ログも維持。
- [x] 一時停止画面へ2つ目の広告ボタンを追加。実広告SDKを考慮して完了処理は
  メインスレッドへ戻し、成功時は`pausedLog`も消去。
- [x] 4文字の`personalityCode`を既存4軸の順位から生成し、傾向カードへ表示。
- [x] iPhone 17e Simulator UIテストで、早抜け→やり直し→再早抜け→続きから→
  上限到達→結果保存→同日復元まで一周。
- [x] やり直し直後のSimulator画像で、旅人がスタートへ戻り、周辺以外が霧に
  覆われ、体力が1000へ戻っていることを確認。

今回追加した3テストは、状態リセットと広告共有上限、SpriteKit上の宝箱・霧・
旅人・光・カメラ、性格コードの順位を直接検証している。接続中のiPhone 17向け
署名ビルドとインストールは成功したが、端末がロック中で起動をiOSに拒否されたため、
今回の実機画面での最終目視と触感は未判定。

ZIP側に含まれていた、ワープ出口まで解決する体力停止、ゴール優先、現在結果優先、
SwiftData診断ログ、音／ハプティクス分離、アクセシビリティ、広告競合防止の退行は
取り込んでいない。

## 今回の追加分

- [x] `stepDuration` は 0.085秒から0.13秒へ変更済み。
- [x] 足音3種は旧版に対して波形のRMS／ピークがほぼ50%（約-6 dB）。
  - a: RMS 1526.3 → 763.0、peak 4349 → 2174
  - b: RMS 1452.6 → 726.1、peak 3873 → 1936
  - c: RMS 1529.9 → 764.8、peak 4313 → 2156
- [x] タイルの色乗せは `SKSpriteNode + colorBlendFactor`。初期シーン生成は約0.045秒、初期ノード数は2601未満。
- [x] ホタルは24個以下。
- [x] 体力は物理移動1マスごとに1減り、ワープ出口の瞬間移動では減らない。
- [x] 残量0になった正確なマスで滑走を止め、早抜けと同じ一時停止画面へ進む。
- [x] ゴール到達と残量0が同じマスなら `reachedGoal == true` を優先。
- [x] ワープ入口で残量0になった場合は、無消費の出口移動を解決してから終了。
- [x] HUDに旅人、体力、ミニマップを表示。
- [x] ミニマップへ渡す座標は `game.explored` と完全一致し、`walked` はその部分集合。迷路全体は渡していない。
- [x] iPhone 17eの画面でHUDが下端へはみ出さず、ホームインジケーター用安全領域を侵食しない。

## フレームレート

- 構造確認では、重い色乗せノードがすべて `SKSpriteNode` になり、51x51全2601マスの一括生成も行われていない。
- シミュレータの自動スワイプと画面録画では、目視できる停止や破綻はなかった。
- iPhone 16上で `Game Performance Overview` を10秒記録し、トレースを `Validation/KareMichiStamina-GamePerformance.trace` に保存した。
- ただし端末側でUIテスト認証がキャンセルされたため、実機で移動中の入力を発生させられず、トレースは静止画面のみ。実機の移動中FPSが旧版より改善した、という比較判定まではできていない。

## 生成の健全性

- [x] 30日分すべて51x51。
- [x] 30日分すべて2x2の開放ブロックなし。
- [x] 30日分すべてゴール到達可能。
- [x] 同一seedの全セル表現が完全一致。
- [x] 翌日のseedでは全セル表現が変化。
- [x] 東京／ロサンゼルスのローカル日付で異なる日付seedになる。
- [x] 5旅人すべての探索画面で赤い完全性警告なし。

## フロー・演出・保存

- [x] 初回旅人選択、気分スキップ、探索、早抜け、一時停止、広告再開、霧スキップ、結果まで通過。
- [x] 結果カード5枚を横スワイプ可能。
- [x] 同日再起動で保存済み結果を直接表示。
- [x] 宝箱の開封画像／光、ワープ画像／回転／対出口、ゴール旗／パルス／完了コールバックを確認。
- [x] 4軸、12人格、短い履歴の道しるべ／旅人回答が安全。
- [x] SwiftDataで配列、Optional、軌跡、診断根拠ログを保存・復元。

## 音

- [x] 12個のWAVを `Assets.xcassets` の外に配置。
- [x] 12個すべてをアプリターゲットの Copy Bundle Resourcesへ登録。
- [x] 実機用 `.app` の直下に12個すべて存在。
- [x] `Bundle.main.url(forResource:)` で取得でき、AVFoundationで全件デコード可能。
- [x] BGM、風、各イベント、UIタップ、カードめくりの呼び出し経路を確認。
- [x] `audioEnabled` はすべての音の入口で参照。
- [ ] 実機スピーカーでの足音の主観的な大きさ、ループ境界、ハプティクスは端末操作認証が得られず未確認。

`footstepStride` は受領値の2を維持した。0.13秒×2マスで最大約3.85回/秒だが、音源自体が約-6 dBになっているため、実機で「連打でうるさい」と確認できるまでは3〜4へ変更していない。

## 検証中に修正した点

1. `StaminaGauge.max` が標準関数 `max` を隠し、コンパイルできない箇所を `Swift.max` / `Swift.min` へ修正。
2. 体力切れ後も一本道を最後まで滑走する問題を、各マスのアニメーション完了時判定へ修正。
3. ゴールと体力切れの同時成立、およびワープ入口での体力切れの優先順位を明示。
4. HUDのbottom safe area無視を外し、ホームインジケーター機種でのはみ出しを防止。
5. 体力とミニマップへVoiceOverラベル／値を追加。
6. 保存直後に現在結果が「今日は、もう歩きました」へ置き換わる画面分岐を修正。
7. 再起動後も診断結果を維持できるよう、判断時間・行き止まり・引き返し・前進回数をSwiftDataへ保存。
8. UIタップ／カードめくりの仕様外ハプティクスを削除し、スキップ操作のタップ音を補完。
9. `hapticsEnabled == false` でも発見音が止まらないよう、音と振動の条件を分離。

## スクリーンショット

- `Screenshots/maze-hud-iphone17e.png`: 探索画面と70/30 HUD
- `Screenshots/result-trail-iphone17e.png`: 早抜け後の軌跡カード
- `Screenshots` 内に5旅人と全フローのUIテスト添付画像も保存
- `Screenshots-Restart/0CF4C690-926E-4663-9C62-F65101C731D7.png`:
  「はじめからやり直す」直後のスタート地点・霧・体力1000
